---
import RichText from '../content/RichText.astro';
import {defaultSiteContent} from '../../content/defaultContent';
import type {FaqItem} from '../../content/types';

interface Props {
  faqs?: FaqItem[];
}

const {faqs = defaultSiteContent.faqs} = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section
  id="faq"
  class="about-paper-texture text-ink"
  aria-labelledby="faq-title"
>
  <div
    class="content-shell section-title-container py-16 sm:py-20 lg:py-28"
  >
    <h2
      id="faq-title"
      class="section-title-tile section-title-tile-faq mx-auto -rotate-1 shadow-xl"
      data-stamp-title
    >
      <span class="sr-only">FAQs</span>
      <img
        class="section-title-art title-palette-blue"
        src={`${base}/images/section_names_img/faqs-cropped.webp`}
        srcset={`${base}/images/section_names_img/faqs-cropped-169.webp 169w, ${base}/images/section_names_img/faqs-cropped.webp 338w`}
        sizes="(min-width: 80rem) 12.7rem, (min-width: 40rem) 17vw, 22vw"
        alt=""
        width="338"
        height="200"
        loading="lazy"
        aria-hidden="true"
      />
    </h2>

    <div class="mt-12">
      {
        faqs.map(({ question, answer }) => (
          <details class="faq-item group">
            <summary class="faq-question flex cursor-pointer list-none items-center justify-between gap-6 py-5 focus-visible:rounded-sm focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-ink">
              <h3 class="min-w-0 text-xl leading-snug sm:text-2xl">
                {question}
              </h3>
              <span
                class="relative size-11 shrink-0 sm:size-12"
                aria-hidden="true"
              >
                <img
                  class="title-palette-blue size-full object-contain group-open:hidden"
                  src={`${base}/images/faq-plus-96.webp`}
                  srcset={`${base}/images/faq-plus-96.webp 96w, ${base}/images/faq-plus.webp 256w`}
                  sizes="(min-width: 40rem) 3rem, 2.75rem"
                  alt=""
                  width="256"
                  height="256"
                />
                <img
                  class="title-palette-blue hidden size-full object-contain group-open:block"
                  src={`${base}/images/faq-close-96.webp`}
                  srcset={`${base}/images/faq-close-96.webp 96w, ${base}/images/faq-close.webp 256w`}
                  sizes="(min-width: 40rem) 3rem, 2.75rem"
                  alt=""
                  width="256"
                  height="256"
                />
              </span>
            </summary>

            <div class="pb-6">
              <div
                class="faq-answer-paper dark-paper-texture p-5 text-paper shadow-lg sm:p-6"
              >
                <RichText
                  class="max-w-[75ch] text-base leading-relaxed sm:text-lg"
                  value={answer}
                />
              </div>
            </div>
          </details>
        ))
      }
    </div>
  </div>
</section>
